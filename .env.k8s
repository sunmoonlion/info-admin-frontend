VITE_PWA_DEBUG=false
VITE_MOCK_ENABLE=false
VITE_APP_NAME=Info Admin

# Production browser traffic is same-origin. The Info Admin IngressRoute
# sends /api to info-admin-backend:8000, keeping CSP connect-src 'self' and
# session/CSRF cookies on the frontend origin.
VITE_API_URL=
