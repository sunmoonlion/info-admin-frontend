VITE_MOCK_ENABLE=false
VITE_APP_NAME=Info Admin

# Production browser traffic is same-origin; the frontend IngressRoute sends
# /api to info-admin-backend:8000.
VITE_API_URL=

# Local development may override VITE_API_URL for a port-forwarded BFF;
# production builds must keep the same-origin default above.
